**Date:** 2026-08-17 (PT)
**Repo/branch:** braintied/watchtower · main
**What this is:** Human README and agent README for the public capture client

The public README now names the product Braintied Watchtower and says
`ora-watchtower` is only the Fly hostname. AGENTS.md replaces the
unfilled harness stub (`{{ONE_LINE_IDENTITY}}`). Both files are
generated by `packages/watchtower/scripts/sync-oss.mjs` so the next
package publish cannot wipe them back to the thin capture blurb.

CLAUDE.md and docs/architecture.md in this repo are no longer template
scaffolding.

Did not rename the Fly app. Did not publish a second npm package.
`@braintied/watchtower` 5.0.1 on GitHub Packages is already the
installable fleet package.
